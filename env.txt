# --- EDUMALL MASTER KEY CABINET ---

# 1. Database Credentials
DB_HOST=localhost
DB_PORT=5432
DB_NAME=edumall_sovereign
DB_USER=master_admin
DB_PASS=REPLACE_WITH_SECURE_PASSWORD

# 2. Cryptographic Vault Keys (The Sovereign Keys)
# These must be 32+ character high-entropy strings
DB_PII_VAULT_KEY=REPLACE_WITH_SECRET_PII_KEY
DB_STAFF_VAULT_KEY=REPLACE_WITH_SECRET_STAFF_KEY
app.vault_key=REPLACE_WITH_SECRET_EXAM_KEY

# 3. Anonymization Salts
REFERRAL_SALT=REPLACE_WITH_GROWTH_SALT
ANALYTICS_SALT=REPLACE_WITH_WAREHOUSE_SALT

# 4. API & Security
JWT_SECRET=REPLACE_WITH_OAUTH_SECRET
WEBHOOK_SECRET=REPLACE_WITH_LIVE_CLASS_SECRET

# 5. Global Grid Settings
CURRENT_NODE_ID=NODE_INDIA_01
AUTO_GEOIP_UPDATE=true